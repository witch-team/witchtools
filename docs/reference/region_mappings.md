# Default regional mapping

A collection of regional mappings for 250 ISO3.

## Usage

``` r
region_mappings
```

## Format

A named list of data.tables with 2 columns

- mapping_name:

  Mapping name (example, witch17), contains the region names in lower
  case

- iso3:

  ISO3 code in upper case
